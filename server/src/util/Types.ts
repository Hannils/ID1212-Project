export interface User {
    id: string,
    username: string,
    email: string
}

export interface Document {
    id: string,
    title: string,
    modified: string,
    created_at: string,
    owner: string
}
